# 核心依赖
openai>=1.0.0
aiohttp>=3.9.0
aiofiles>=23.0.0
pydantic>=2.0.0
pyyaml>=6.0

# 异步和工具
asyncio-mqtt>=0.16.0  # 用于异步消息
python-multipart>=0.0.6  # 文件上传

# 开发工具
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-benchmark>=4.0.0
hypothesis>=6.0.0
black>=23.0.0
mypy>=1.5.0

# 其他
tiktoken

# 可选：向量数据库（用于高级上下文管理）
# chromadb>=0.4.0
# sentence-transformers>=2.2.0
# pandas   # 初始可不必安装，运行时aacode会自动安装
# openpyxl
# xlrd

# 可选：沙箱环境
# docker>=6.0.0
# paramiko>=3.0.0  # SSH连接